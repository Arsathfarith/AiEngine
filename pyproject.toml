[project]
name = "AiEngine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Flask>=2.3",
    "pandas>=2.0",
    "numpy>=1.25",
    "scikit-learn>=1.4",
    "python-dotenv>=1.0",
    "PyPDF2>=3.0",
    "python-docx>=0.9.0"
]

[tool.vercel]
entrypoint = "app:app"
